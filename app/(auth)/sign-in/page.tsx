import AuthProvider from "../(components)/(authProvider)"

const SignIn = () => {
    return (
        <AuthProvider>
            <section>
                <h1>Form SignIn</h1>
            </section>
        </AuthProvider>
        
    )
}

export default SignIn